## 项目主要需求

### 项目概述

开发一个智能灌溉管理系统的后端 API 服务，面向庭院花园和社区绿化场景，通过传感器数据采集和智能算法实现自动化灌溉调度，提供设备管理、灌溉计划、数据分析等 RESTful API 接口。

### 功能需求

1. **设备注册与管理**：提供灌溉设备（电磁阀、水泵、土壤湿度传感器、雨量传感器等）的 CRUD 接口，支持设备分组（按灌溉区域），设备状态在线/离线检测，设备心跳上报机制。
2. **传感器数据采集接口**：接收设备上报的土壤湿度、环境温度、降雨量等传感器数据，数据存入时序数据库（使用 PostgreSQL 的 TimescaleDB 扩展），支持按设备、时间范围查询历史数据。
3. **灌溉计划管理**：支持创建定时灌溉计划（指定开始时间、持续时长、重复周期）、条件灌溉计划（当土壤湿度低于阈值时自动触发），计划的增删改查和启用/禁用接口。
4. **智能灌溉调度引擎**：后台定时任务检查灌溉计划，结合当前土壤湿度数据和天气预报数据（模拟）决定是否执行灌溉，避免雨天自动灌溉，生成灌溉执行日志。
5. **灌溉执行记录**：记录每次灌溉的执行时间、持续时长、触发方式（手动/定时/条件）、用水量估算等，支持按时间范围和区域统计灌溉历史。
6. **用水量统计与分析**：提供日/周/月用水量统计接口，各区域用水占比分析，与历史同期对比，节水建议生成。
7. **告警通知接口**：设备离线告警、传感器异常告警（数据超范围）、灌溉失败告警，告警记录的查询和处理状态更新接口。
8. **系统配置管理**：灌溉区域配置、设备参数配置、用户偏好设置（灌溉时段偏好、节水模式等）的管理接口。

### 技术要求

- 后端：Go 1.21 + Gin 框架
- ORM：GORM
- 数据库：PostgreSQL（主数据存储）
- 缓存：Redis（设备状态缓存、计划调度队列）
- API 文档：Swagger（Swaggo 自动生成）
- 认证：JWT
- 后台任务：Go 协程 + 定时器（灌溉调度引擎）
- 日志：Zap 结构化日志
- 配置管理：Viper

## 代码存放规则

这是后端项目，无前端代码。

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

API 文档放在 `docs/`（Swagger 自动生成目录）。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排后端、数据库及依赖服务
- `backend/Dockerfile`：后端项目构建与运行镜像
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 端口映射（本项目分配的端口）：
  - 后端：3109:8080
  - PostgreSQL：5609:5432
  - Redis：6509:6379
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（后端启动命令）
- API 文档地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

